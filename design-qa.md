# GLE 授权范围与确认入口 UX 验收

## 目标

- GLE 顶部待确认数字、确认入口和确认列表使用同一份 exact `ad_id` 授权范围。
- 只允许五个 GLE 授权账户中的广告进入 GLE 操作队列。
- “分析与历史”保留全量经营分析，但明确区分 GLE 授权广告与范围外广告。
- 打开确认列表只切换本地 UI，不产生 Meta 写入。

## 参考问题

用户截图显示“分析与历史”的全局待确认列表将范围外广告统一标成“到 GLE 工作台确认”，而 GLE 顶部实际只统计授权范围，造成 21 与 6 的口径冲突。

## 设计决策

- 授权范围唯一来源：`gle-ad-coverage` 返回的五个账户及其 exact `ad_id`。
- 授权且待确认：显示可点击的“进入 GLE 确认”。
- 未授权：显示中性只读标签“未纳入 GLE · 仅供分析”，不提供 GLE 操作入口。
- 已有关联但不可操作的任务不再遮蔽更新的经营方案；可操作任务仍优先。

## 验收清单

- [x] 自动化测试验证 6 条授权待确认与 15 条范围外建议被正确分流。
- [x] 自动化测试验证顶部与确认列表的待确认数都为 6。
- [x] 自动化测试验证非操作型既有任务不遮蔽新的暂停建议。
- [x] 自动化测试验证 GLE 覆盖载入后会重绘“分析与历史”范围标签。
- [ ] Chrome 生产页面验证五账户范围、数字一致、范围外无 GLE 入口。
- [ ] Chrome 点击“进入 GLE 确认”验证展示对应授权广告且不触发 Meta 写入。

## 安全边界

- 本变更仅修改页面路由与显示，不新增 API、数据库写入或 Meta 执行动作。
- 真正修改 Meta 仍需进入既有方案确认和受控执行链路。
